4.7.7 估算順序
      Order of Evaluation

  JavaScript 總是用嚴格的左到右順序去估算運算式，
舉例來說，在運算式 w = x + y * z 中，
子運算式 w 會先被估算，接者是 x y z。

  然後 y 與 z 的植被相乘，加到 x 的值上，
隨後指定至由 w 所指定的變數或特性。

  在運算式中加入括號能改變乘 加 與指定的順序，
但不能改變從左至又的估算順序。